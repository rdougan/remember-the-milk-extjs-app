= Remember the Milk ExtJS Application

Will be totally built using ExtJS and the Ext MVC framework by edspencer. Will be moved into an Adobe AIR application at some point.

Waiting for RTM to implement JSON requests.